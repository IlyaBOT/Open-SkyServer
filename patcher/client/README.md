# Patched client release staging

This directory is intentionally not populated by Git.

After producing and validating a patched Skype client locally, place the release-ready client files here and package them for a GitHub tag/release. The `.gitignore` prevents the client binary, profiles, logs and generated credentials from being committed accidentally.

Do not place server private keys in a client package.
